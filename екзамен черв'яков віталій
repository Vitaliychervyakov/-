pub fn main() {
    // Початкове значення змінної
    var i: int = 0;

    // Цикл, який працює безкінечно
    while true {
        // Вивід значення змінної
        println(i);

        // Збільшення значення змінної
        i = i + 1;

        // Перевірка умови завершення циклу
        if i > 10 {
            // Вихід із циклу, якщо умова виконана
            break;
